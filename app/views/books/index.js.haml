$("#books").append("#{ j render @books }")
$("#load_more").replaceWith("#{j (link_to t('.view_more'), books_path(page: @books.next_page), remote: true, id: 'load_more', class: 'btn btn-primary') if @books.next_page }");
